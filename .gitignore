# Tudo que é da máquina de quem roda: os murais configurados, o histórico de
# cada um, snapshots e caches do onboarding. Nada disso pode ir para o
# repositório — murais.json aponta para conversas específicas e os tasks.json
# contêm o texto de mensagens internas.
data/

# Build da interface. Quem clona roda `npm install && npm run build`.
dist/
node_modules/
*.tsbuildinfo

*.log
.DS_Store
Thumbs.db
